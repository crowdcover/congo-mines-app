<%= javascript_tag do %>
  $(function() {
    app.initMap();
    var geodata_url = "<%= get_geodata_drc_company_path(@drc_company) %>"

    app.loadCompanyData(geodata_url);
    <!-- $.ajax({
      url: "<%= get_geodata_drc_company_path(@drc_company) %>",
      type: "GET",
      dataType: 'json',
      success: function(companyGeodata) {
        console.log('got the geodata. inside success callback.');
        console.log(this);
        console.log(companyGeodata);
        //var  = companyGeodata;
        app.initMap(companyGeodata);
      }
    });
    console.log('outside of ajax call');
    console.log(this); -->
  });
<% end %>

// javascript:
//   $(function() {
//     app.initMap();
//     // run jquery ajax
//     // for success callback, run map get geoJson
//     $.ajax({
//       url: "<%= get_geodata_drc_company_path(@drc_company) %>",
//       type: "GET",
//       success: function(geodata) {
//         console.log('got the geodata! looks like:');
//         console.log(geodata);
//       }
//     });
//
//     $(".accordion").on("click", "li", function (event) {
//        $(this).find(".content").slideToggle(300);
//      });
//   });
