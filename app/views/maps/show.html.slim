div.content.map
  div.row
    /div.large-12.columns

  = render "map_nav"
  div#map

javascript:
  $(function() {
    app.initMap();
    var geodataUrl = "#{get_all_deposits_map_url}"
    app.getGeodata(geodataUrl);
  });
